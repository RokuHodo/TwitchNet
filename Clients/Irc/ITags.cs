namespace
TwitchNet.Clients.Irc
{
    public interface
    ITags
    {
        /// <summary>
        /// Whether or not tags are atached to the message.
        /// </summary>
        bool exist { get; }
    }
}
